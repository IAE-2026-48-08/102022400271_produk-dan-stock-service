services:
  app:
    image: php:8.2-cli
    container_name: product-stock-service
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "8000:8000"
    command: >
      sh -c "
      php artisan serve --host=0.0.0.0 --port=8000
      "